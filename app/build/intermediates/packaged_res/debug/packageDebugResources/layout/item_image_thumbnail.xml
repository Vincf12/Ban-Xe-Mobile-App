<?xml version="1.0" encoding="utf-8"?>
<!-- item_image_thumbnail.xml -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="60dp"
    android:layout_height="60dp"
    android:layout_marginEnd="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/img_thumbnail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop" />

        <!-- Selected overlay -->
        <View
            android:id="@+id/view_selected_overlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#80000000"
            android:visibility="gone" />

        <!-- Selected indicator -->
        <ImageView
            android:id="@+id/img_selected_indicator"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:src="@drawable/rounded_image"
            android:tint="@android:color/white"
            android:visibility="gone" />

        <!-- Border for selected state -->
        <View
            android:id="@+id/view_selected_border"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/border_selected_thumbnail"
            android:visibility="gone" />

    </FrameLayout>

</androidx.cardview.widget.CardView>